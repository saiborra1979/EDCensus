library(ggplot)
install.packages('cowplot')
install.packages('data.table')
install.packages()
installed.packages()
head(installed.packages())
data.frame(installed.packages())
pckgs <- data.frame(installed.packages())
head(pckgs)
pckgs$Package
as.character(pckgs$Package)
pckgs$Package <- as.character(pckgs$Package)
pckgs$Package
duplicated(pckgs$Package)
needed <- c('cowplot','data.table')
pckgs <- data.frame(installed.packages())
pckgs$Package <- as.character(pckgs$Package)
for (need in needed) {
print(need)
}
need %in% pckgs$Package
for (need in needed) {
if (need %in% pckgs$Package) {
library(need,character.only = T)
} else {
install.packages(need)
}
}
needed <- c('cowplot','data.table', 'gganimate')
pckgs <- data.frame(installed.packages())
pckgs$Package <- as.character(pckgs$Package)
for (need in needed) {
if (need %in% pckgs$Package) {
library(need,character.only = T)
} else {
install.packages(need)
}
}
needed <- c('cowplot','data.table', 'gganimate')
pckgs <- data.frame(installed.packages())
pckgs$Package <- as.character(pckgs$Package)
for (need in needed) {
if (need %in% pckgs$Package) {
library(need,character.only = T)
} else {
install.packages(need)
}
}
getwd()
dir_base <- getwd()
file.path(dir_base,'..')
file.path(dir_base,'..','output')
list.files(file.path(dir_base,'..','output'))
list.files(file.path(dir_base,'..','output','flow'))
list.files(file.path(dir_base,'..','output','flow','test'))
list.files(file.path(dir_base,'..','output','flow'))
dir_flow <- file.path(dir_base,'..','output','flow')
